// Minimal translation unit so the Metal backend OBJECT library always has a
// .cpp TU (implementation is mostly in .mm files).

namespace eng {
[[maybe_unused]] void metalRenderBackendModuleAnchor() {}
}  // namespace eng
