#pragma once

namespace eng {

// Context for evaluating expressions (provides property access)
struct EvalContext {
  // TBD: Will be populated by game layer with entity, world, scenario
  // references For Phase 1, this is a placeholder
};

}  // namespace eng
